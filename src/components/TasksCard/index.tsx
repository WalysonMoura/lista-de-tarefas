import { api } from "@/lib/axios";
/*

import * as Popover from "@radix-ui/react-popover";
import { Check } from "phosphor-react";
import { FormEvent, useState } from "react";
import * as Style from "./style";

export default function TaskCard({
  content,
  onEditTask,
  onDeleteTask,
}: TaskContent) {
  return (
    <Style.Conteiner>
      {content}

      <Popover.Root>
        <Popover.Trigger>
          <Style.EditButton>Editar</Style.EditButton>
        </Popover.Trigger>

        <Popover.Portal>
          <Popover.Content>
            <label htmlFor="title">Editar Tarefa</label>
            <input
              type="text"
              id="title"
              autoFocus
              defaultValue={content}
              onChange={(event) => onEditTask(event.target.value)}
            />
          </Popover.Content>
        </Popover.Portal>
      </Popover.Root>

      <Style.DeleteButton onClick={onDeleteTask}>Excluir</Style.DeleteButton>
    </Style.Conteiner>
  );
}
*/